<%- include("partials/head.ejs") %> 

  </head>
  <body>
    <%- include("partials/nav.ejs") %>
    <main>
      <div class="user-list">
        <h1>Users</h1>
        <% if (userList.length > 0) { %>
        <ul>
          <% for (let user of userList) { %>
          <li><%= user.name %></li>
          <% } %>
        </ul>
        <% } %>
      </div>
    </main>
  </body>
</html>
